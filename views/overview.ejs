<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plant Sightings</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }
        .header {
            background-color: #4CAF50;
            color: white;
            padding: 20px 0; /* Reduced padding at the top and bottom */
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 0 50px;
            align-items: center;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            padding: 5px 10px;
        }
        .navbar ul {
            list-style-type: none;
            display: flex;
            gap: 10px;
            padding: 0;
            margin: 0;
        }
        .title-section {
            text-align: center;
            padding: 100px; /* Adjust this padding to control the space around the image and text */
        }
        .title-section img {
            width: 175px; /* Set this to the width of your custom image */
            height: auto; /* Maintain aspect ratio */
            margin: 10px auto; /* Center the image */
        }
        .title-section h1 {
            margin: 10px 0;
        }
        .filter-bar {
            text-align: center;
            padding: 20px;
        }
        /* 过滤条按钮的样式 */
        .filter-bar button {
            background: #ffffff; /* 设置按钮背景为白色 */
            border: 2px solid #4CAF50; /* 设置边框颜色为标题栏相同的绿色 */
            color: #4CAF50; /* 设置文本颜色为绿色 */
            padding: 10px 20px; /* 按钮内边距 */
            margin: 0 10px; /* 按钮外边距 */
            font-weight: bold; /* 文本加粗 */
            border-radius: 5px; /* 边框圆角效果 */
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s; /* 过渡动画效果 */
            width: 100px; /* 统一按钮宽度 */
            height: 40px;
            box-sizing: border-box; /* box-sizing 设置为 border-box 以包含边框和内边距 */
            display: inline-block; /* 使按钮按内联块级元素渲染 */
        }

        /* 当鼠标悬浮在按钮上时的样式 */
        .filter-bar button:hover {
            background: #4CAF50; /* 鼠标悬浮时背景颜色变为绿色 */
            color: #ffffff; /* 鼠标悬浮时文本颜色变为白色 */
            border: 2px solid #ffffff; /* 鼠标悬浮时边框颜色变为白色 */
        }
        .content {
            padding: 20px;
            display: flex;
            justify-content: center;
        }
        .card {
            border: 1px solid #ccc;
            padding: 20px;
            margin: 75px;
            text-align: center;
            background-color: white;
            border-radius: 10px;
        }
        .card img {
            width: 150px;
            height: 150px;
            background-color: #ddd;
        }
        /* 导航按钮的基础样式 */
        .nav-button {
            padding: 5px 10px;
            display: inline-block; /* 使链接像按钮一样显示 */
            transition: background-color 0.3s, color 0.3s; /* 添加过渡动画效果 */
            border-radius: 5px; /* 轻微的圆角效果 */
        }

        /* 鼠标悬浮在导航按钮上时的样式 */
        .nav-button:hover {
            background-color: rgba(255, 255, 255, 0.2); /* 添加淡白色背景 */
            color: #ffffff; /* 文本颜色保持不变，即使在淡白色背景上也能看清 */
        }
        /* 排序下拉菜单样式 */
        .sort-dropdown {
            padding: 10px 20px;
            margin-left: 10px; /* 与其他按钮保持一致的外边距 */
            border: 2px solid #4CAF50; /* 边框颜色 */
            border-radius: 5px; /* 圆角效果 */
            background-color: white;
            color: #4CAF50;
            cursor: pointer;
            outline: none; /* 去掉点击时的轮廓 */
            width: 120px; /* 统一按钮宽度 */
            height: 40px;
        }
        .clearfix {
            clear: both;
        }

    </style>
</head>
<body>

<div class="header">
    <!-- Navigation Bar -->
    <div class="navbar">
        <div class="logo"><a href="/">Plant Sightings</a></div>
        <ul>
            <li><a href="/home" class="nav-button">Home</a></li>
            <li><a href="/overview" class="nav-button">Overview</a></li>
            <li><a href="#" class="nav-button">Add</a></li>
            <li><a href="https://github.com/captainLegoo/TeamMSc09" class="nav-button"><img style="width: 20px; height: 20px;" src="/images/github_logo.png" alt="GitHub"></a></li>
        </ul>
    </div>

    <!-- Title Section with Customizable Image and Text -->
    <div class="title-section">
        <img src="/images/plant_logo.png" alt="Customizable Image">
        <h1>OverView</h1>
        <p>Your customizable text here</p>
    </div>
</div>

<!-- Filter Section -->
<div class="filter-bar">
    <select class="sort-dropdown" id="sortDropdown">
        <option value="date">按日期排序</option>
        <option value="name">按名称排序</option>
        <option value="distance">按距离排序</option>
    </select>
</div>


<!-- Content Section -->
<div class="content" style="display: flex; flex-wrap: wrap; justify-content: flex-start;">
    <% plants.forEach((plant, index) => { %>
        <div class="card">
            <img src="/images/plant_logo.png" alt="Placeholder image for plant sighting">
            <p><%= plant.description %></p>
            <p><%= plant.identification.name %></p>
        </div>
        <% if ((index + 1) % 4 === 0) { %>
            <div style="flex-basis: 100%; height: 0;"></div>
        <% } %>
    <% }) %>
</div>

<script>
    document.getElementById('sortDropdown').addEventListener('change', function() {
        var sortValue = this.value;

        if (sortValue === 'distance') {
            // 使用HTML5 Geolocation API获取用户的当前位置
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(function(position) {
                    var lat = position.coords.latitude;
                    var lng = position.coords.longitude;
                    // 重定向到服务器并附加排序方式和位置信息
                    window.location.href = `/overview?sort=${sortValue}&lat=${lat}&lng=${lng}`;
                }, function() {
                    alert('无法获取您的位置信息，请选择其他排序方式。');
                });
            } else {
                alert('您的浏览器不支持地理定位，无法使用按距离排序。');
            }
        } else if (sortValue) {
            // 对于非距离排序，只需重定向并附加排序方式
            window.location.href = `/overview?sort=${sortValue}`;
        }
    });
</script>

</body>
</html>