<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plant Sightings</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }
        .header {
            background-color: #4CAF50;
            color: white;
            padding: 20px 0; /* Reduced padding at the top and bottom */
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 0 50px;
            align-items: center;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            padding: 5px 10px;
        }
        .navbar ul {
            list-style-type: none;
            display: flex;
            gap: 10px;
            padding: 0;
            margin: 0;
        }
        .title-section {
            text-align: center;
            padding: 100px; /* Adjust this padding to control the space around the image and text */
        }
        .title-section img {
            width: 175px; /* Set this to the width of your custom image */
            height: auto; /* Maintain aspect ratio */
            margin: 10px auto; /* Center the image */
        }
        .title-section h1 {
            margin: 10px 0;
        }
        .filter-bar {
            text-align: center;
            padding: 20px;
        }
        /* 过滤条按钮的样式 */
        .filter-bar button {
            background: #ffffff; /* 设置按钮背景为白色 */
            border: 2px solid #4CAF50; /* 设置边框颜色为标题栏相同的绿色 */
            color: #4CAF50; /* 设置文本颜色为绿色 */
            padding: 10px 20px; /* 按钮内边距 */
            margin: 0 10px; /* 按钮外边距 */
            font-weight: bold; /* 文本加粗 */
            border-radius: 5px; /* 边框圆角效果 */
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s; /* 过渡动画效果 */
            width: 100px; /* 统一按钮宽度 */
            height: 40px;
            box-sizing: border-box; /* box-sizing 设置为 border-box 以包含边框和内边距 */
            display: inline-block; /* 使按钮按内联块级元素渲染 */
        }

        /* 当鼠标悬浮在按钮上时的样式 */
        .filter-bar button:hover {
            background: #4CAF50; /* 鼠标悬浮时背景颜色变为绿色 */
            color: #ffffff; /* 鼠标悬浮时文本颜色变为白色 */
            border: 2px solid #ffffff; /* 鼠标悬浮时边框颜色变为白色 */
        }
        .content {
            padding: 20px;
            display: flex;
            justify-content: center;
        }
        .card {
            border: 1px solid #ccc;
            padding: 20px;
            margin: 0 10px;
            text-align: center;
            background-color: white;
        }
        .card img {
            width: 150px;
            height: 150px;
            background-color: #ddd;
        }
        /* 导航按钮的基础样式 */
        .nav-button {
            padding: 5px 10px;
            display: inline-block; /* 使链接像按钮一样显示 */
            transition: background-color 0.3s, color 0.3s; /* 添加过渡动画效果 */
            border-radius: 5px; /* 轻微的圆角效果 */
        }

        /* 鼠标悬浮在导航按钮上时的样式 */
        .nav-button:hover {
            background-color: rgba(255, 255, 255, 0.2); /* 添加淡白色背景 */
            color: #ffffff; /* 文本颜色保持不变，即使在淡白色背景上也能看清 */
        }
        /* 排序下拉菜单样式 */
        .sort-dropdown {
            padding: 10px 20px;
            margin-left: 10px; /* 与其他按钮保持一致的外边距 */
            border: 2px solid #4CAF50; /* 边框颜色 */
            border-radius: 5px; /* 圆角效果 */
            background-color: white;
            color: #4CAF50;
            cursor: pointer;
            outline: none; /* 去掉点击时的轮廓 */
            width: 120px; /* 统一按钮宽度 */
            height: 40px;
        }
    </style>
</head>
<body>

<div class="header">
    <!-- Navigation Bar -->
    <div class="navbar">
        <div class="logo">Plant Sightings</div>
        <ul>
            <li><a href="#" class="nav-button">Home</a></li>
            <li><a href="#" class="nav-button">Overview</a></li>
            <li><a href="#" class="nav-button">Add</a></li>
            <li><a href="#" class="nav-button">Github</a></li>
        </ul>
    </div>

    <!-- Title Section with Customizable Image and Text -->
    <div class="title-section">
        <img src="/images/plant_logo.png" alt="Customizable Image">
        <h1>OverView</h1>
        <p>Your customizable text here</p>
    </div>
</div>

<!-- Filter Section -->
<div class="filter-bar">
    <button>All</button>
    <button>B</button>
    <select class="sort-dropdown" onchange="sortImages(this.value);">
        <option value="default">默认排序</option>
        <option value="date">按日期排序</option>
        <option value="name">按名称排序</option>
    </select>
</div>

<!-- Content Section -->
<div class="content">
    <div class="card">
        <img src="https://placehold.co/150" alt="Placeholder image for plant sighting">
        <p>===描述===</p>
    </div>
    <!-- Repeat the card as needed -->
</div>

</body>
</html>
